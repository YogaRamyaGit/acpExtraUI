const style: { [key: string]: React.CSSProperties } = {
    pageContainer: { padding: '0 15px' },
    topBarContainer: { display: 'flex', justifyContent: 'space-between' as 'space-between' },
    tableContainer: { margin: '10px 0' }
};

export default style;
