const style: {[key: string]: React.CSSProperties} = {
    iconContainer: { position: 'relative' },
    deleteButton: { position: 'absolute', right: 0 },
    configCard: { margin: '10px 0 20px 0', padding: 15, paddingTop: 0 }
};

export default style;
