const style: { [key: string]: React.CSSProperties } = {
    addButton: { margin: '10px 0' },
    buttonsContainer: { display: 'flex' as 'flex', justifyContent: 'space-between' as 'space-between' }
};

export default style;
