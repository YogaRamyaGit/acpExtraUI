const style: {[key: string]: React.CSSProperties} = {
    wrapper: {
        width: '100vw',
        height: '100vh',
        marginTop: -44,
        backgroundImage: 'linear-gradient(to bottom right,#009688, #fff)'
    },
    container: { display: 'flex', justifyContent: 'center' as 'center', position: 'relative', top: '40%' },
    text: { textAlign: 'center' }
};

export default style;
